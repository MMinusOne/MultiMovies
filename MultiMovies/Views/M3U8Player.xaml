<UserControl x:Class="MultiMovies.Views.M3U8Player"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MultiMovies.Views"
             xmlns:vlc="clr-namespace:LibVLCSharp.WPF;assembly=LibVLCSharp.WPF"
             xmlns:vm="clr-namespace:MultiMovies.ViewModels"
             xmlns:cnv="clr-namespace:MultiMovies.Lib.Converters"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <cnv:DurationMSToDisplayableConverter x:Key="DurationMSToDisplayableConverter" />
    </UserControl.Resources>
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <vlc:VideoView Grid.Row="0" MediaPlayer="{Binding Source={x:Static vm:MediaPlayerViewModel.Instance}, Path=Mediaplayer}"/>
        
        <Grid Grid.Row="1" Panel.ZIndex="1000" Background="Transparent" VerticalAlignment="Bottom">
            <Grid.RowDefinitions>
                <RowDefinition Height="50*"/>
                <RowDefinition  Height="50*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="100*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Foreground="{DynamicResource TextPrimary}" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="12" Text="{Binding Source={x:Static vm:MediaPlayerViewModel.Instance}, Path=CurrentTime, Converter={StaticResource DurationMSToDisplayableConverter}}"/>
                <Slider Grid.Column="1" Minimum="0" Maximum="100" Value="{Binding Source={x:Static vm:MediaPlayerViewModel.Instance}, Path=Timeline}">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="PreviewMouseUp">
                            <i:InvokeCommandAction Command="{Binding Source={x:Static vm:MediaPlayerViewModel.Instance}, Path=TimelineRelease}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="PreviewMouseDown">
                            <i:InvokeCommandAction Command="{Binding Source={x:Static vm:MediaPlayerViewModel.Instance}, Path=TimelineLock}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Slider>
            </Grid>

            <Grid  Grid.Row="1" Margin="10,10,10,10" Background="Yellow" Height="50">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5,5,5,5">
                    <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{DynamicResource TextPrimary}" FontWeight="SemiBold" FontSize="12">Volume</TextBlock>
                    <Slider Value="{Binding Source={x:Static vm:MediaPlayerViewModel.Instance}, Path=Volume}" VerticalAlignment="Center" HorizontalAlignment="Center" Minimum="0" Maximum="100" Width="100" Margin="5,5,5,5" />
                </StackPanel>
                <Button Width="50" Height="50" Background="Red" HorizontalAlignment="Center" VerticalAlignment="Center" Command="{Binding Source={x:Static vm:MediaPlayerViewModel.Instance}, Path=PlayPauseCommand}">
                    <Image Source="/Static/play.png" Width="20" Height="20"/>
                </Button>
                
            </Grid>  
        </Grid>

    </Grid>
</UserControl>
