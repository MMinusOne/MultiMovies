<Page x:Class="MultiMovies.Views.SearchPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MultiMovies.Views"
      xmlns:vm="clr-namespace:MultiMovies.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SearchPage">

    <Grid>
        <ScrollViewer>

            <ItemsControl HorizontalAlignment="Left" ItemsSource="{Binding Source={x:Static vm:SearchPageViewModel.Instance}, Path=MoviesResults}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="10,10,10,10" Width="150px" Height="200px" Background="{DynamicResource BackgroundBase200}">
                            <Border CornerRadius="200" BorderBrush="Transparent" BorderThickness="2">
                                <Image Source="{Binding poster_path}" Stretch="Fill" />
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
